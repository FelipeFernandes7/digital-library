import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`
body{
background: linear-gradient(to top, #232323 0%, black 100%) no-repeat center center fixed;
background-size: cover;
color: #ffff;
}
`;
